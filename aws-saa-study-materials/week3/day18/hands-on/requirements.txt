# AWS Lambda 함수 의존성 패키지
# Day 18: API Gateway & Lambda 실습용

# JSON Web Token 처리 (Lambda Authorizer 구현시 사용)
PyJWT==2.8.0

# AWS SDK for Python (Lambda 런타임에 기본 포함되지만 명시적 버전 관리용)
boto3==1.34.0
botocore==1.34.0

# HTTP 요청 처리 (외부 API 호출시 사용)
requests==2.31.0

# 날짜/시간 처리 개선
python-dateutil==2.8.2

# 환경 변수 관리 (개발 환경용)
python-dotenv==1.0.0

# 로깅 개선
structlog==23.2.0

# 데이터 검증 (입력 검증 강화시 사용)
pydantic==2.5.0

# 암호화 관련 (비밀번호 해싱 등)
bcrypt==4.1.2

# UUID 생성 개선
shortuuid==1.0.11

# 설치 방법:
# pip install -r requirements.txt
#
# Lambda 배포시 주의사항:
# 1. Lambda 런타임에 기본 포함된 패키지들은 제외하고 배포
# 2. 패키지 크기가 큰 경우 Lambda Layer 사용 고려
# 3. 실제 사용하는 패키지만 포함하여 배포 패키지 크기 최소화